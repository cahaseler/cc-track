# Code Review: Feature Specification: Migrate cc-track to Claude Code Plugin System

**Task ID:** TASK_101
**Reviewed by:** Codex CLI
**Date:** 2025-10-15T19:04:30.868Z

---

**Findings**
- High: `commands/scripts/hook.ts:1` exports `runHookCommand` but never invokes it when the file is run directly; there is no `if (import.meta.main)` entrypoint. The docs still tell contributors to simulate hooks via `echo … | bun run commands/scripts/hook.ts` (`AGENTS.md:85`), but that command currently no-ops, so local hook validation is broken. Please add a CLI entrypoint that builds default deps (e.g. via `createDefaultCommandDeps`) and funnels the result through `applyCommandResult`, matching the other action commands.

**Optional**
- Consider adding similar entrypoints to other scripts the docs encourage running directly (`commands/scripts/git-session.ts`, `commands/scripts/parse-logs.ts`) so manual workflows remain consistent.

**Notes**
- Review file creation failed: sandbox is read-only, so `code-reviews/TASK_101_2025-10-15_1859-UTC.md` could not be written.
- Tests unchecked here; please rerun `bun run check` / `bun test` after fixes.

Spec reference: `.claude/specs/101-migrate-to-claude-code-plugin-system/spec.md`