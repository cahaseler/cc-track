# Code Review: Feature Specification: TASK_100: Create Templates for Spec-Driven Workflow

**Task ID:** TASK_100
**Reviewed by:** Codex CLI
**Date:** 2025-10-14T22:00:40.585Z

---

- Couldn’t write `code-reviews/TASK_100_2025-10-14_2158-UTC.md` (read-only sandbox); full review content is below.

**Required Fixes**
- **High** – Templates are copied into `.claude/` instead of the spec-required `.claude/templates/specs/`, so downstream tooling cannot locate them (`.claude/specs/100-task-100-create-templates-for-spec-driven-workflow/spec.md:36`, `src/commands/setup-templates.ts:21`, `src/commands/setup-templates.ts:49`). Please emit the files under `.claude/templates/specs/`.
- **High** – `spec-template.md` skips the mandated `Overview` and `Constraints` sections (spec requirement at `.claude/specs/100-task-100-create-templates-for-spec-driven-workflow/spec.md:16`); the file jumps straight from quick guidelines to user stories and never defines constraints (`templates/spec-template.md:8`, `templates/spec-template.md:78`). Add those sections (with Socratic output guidance).
- **High** – `plan-template.md` omits the required `Architecture`, `Technical Approach`, `Implementation Details`, and `Risks` headings (`.claude/specs/100-task-100-create-templates-for-spec-driven-workflow/spec.md:19`); current content stops at technical context and phase breakdowns (`templates/plan-template.md:8-123`). Introduce dedicated sections.
- **High** – `tasks-template.md` lacks the task metadata (status, dependencies, estimates) the spec demands (`.claude/specs/100-task-100-create-templates-for-spec-driven-workflow/spec.md:22-25`); the template only covers numbering/TDD rules (`templates/tasks-template.md:7-40`). Add explicit metadata fields or table per task.
- **High** – Progress template is missing the required `Notes` section (`.claude/specs/100-task-100-create-templates-for-spec-driven-workflow/spec.md:27`); the file has Current Focus, Updates, Blockers, etc., but no Notes area (`templates/progress-template.md:11-200`). Add it.
- **High** – No example `.metadata.json` describing required/optional fields or valid status values exists (`.claude/specs/100-task-100-create-templates-for-spec-driven-workflow/spec.md:32-35`); the embedded bundle only lists the Markdown templates (`src/lib/embedded-resources.ts:6-40`). Add the JSON example and ensure setup installs it.

**Spec Alignment**
- Primary spec: `.claude/specs/100-task-100-create-templates-for-spec-driven-workflow/spec.md:14-38`. Several acceptance criteria remain unmet (storage location, mandatory sections, metadata example).

**Summary**
- Extensive migration toward the spec-driven workflow, but the core templates still violate multiple TASK_100 requirements, keeping the workflow blocked.

**Behavioral Diffs**
- New commands/templates land, yet missing sections and misplacement mean consumers still can’t rely on them.

**Tests & Coverage**
- Not run (read-only environment).

**Risks**
- Leaving issues above unfixed breaks template discovery and fails to provide required authoring guidance, halting the intended workflow.

**Optional Improvements**
- None beyond the blockers above.

**Verified Commands**
- None (execution restricted).