# Plan: 091

Captured: 2025-09-25T00:17:48.751Z

## Plan to Create GitHub Issues for cc-track Bugs

I'll create three GitHub issues for the bugs we discovered:

1. **TypeScript/lint validation only parses output on non-zero exit codes**
   - Problem: Output is only parsed in catch blocks, missing errors when commands exit 0
   - Impact: Validation silently passes even when there are actual errors

2. **Flags incorrectly appended to npm run commands**
   - Problem: Appending `--pretty false --incremental` to npm commands doesn't work
   - Impact: TypeScript output isn't in parseable format

3. **Add integration tests for validation hooks**
   - Problem: No tests verify that actual TypeScript/lint errors are caught
   - Impact: Bugs like these go unnoticed until production use

I'll use the gh CLI to create these issues in the cc-track repository.